# MD013: Line length
# Increased limit to accommodate long URLs and content descriptions.
# Code blocks are excluded since they contain copilot commands and generated code.
MD013:
  line_length: 200
  code_blocks: false
  tables: false
