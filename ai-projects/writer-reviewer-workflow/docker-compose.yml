services:
  writer-reviewer-workflow:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8088:8088"
    command: ["python", "main.py", "--server"]
