# Azure Quantum SDK
azure-quantum[qiskit]>=1.0.0,<2.0.0
# Qiskit pre-1.0 versions retained for compatibility with qiskit_algorithms &
# qiskit_machine_learning (VQC components rely on older circuit library symbols).
# If upgrading to 1.x later, also upgrade qiskit_algorithms when a compatible release
# is published, then remove this downgrade.
qiskit==0.46.0
qiskit-aer>=0.12.0,<0.13.0

# Quantum Machine Learning
pennylane>=0.39.0,<1.0.0
## Pennylane-Qiskit adapter may depend on qiskit-terra APIs. If incompatibility
## arises with Qiskit 1.x, pin below 0.35 or switch to pennylane-braket / other backends.
pennylane-qiskit>=0.39.0,<1.0.0
torch>=2.5.0,<3.0.0
numpy>=1.26.4,<2.0.0
scipy>=1.14.0,<2.0.0
qiskit-machine-learning>=0.6.0,<0.7.0  # Compatible with Qiskit 0.46.x

# Azure Integration
azure-identity>=1.15.0,<2.0.0
azure-core>=1.29.0,<2.0.0

# Data Processing & Visualization
pandas>=2.2.0,<3.0.0
matplotlib>=3.9.0,<4.0.0
scikit-learn>=1.6.0,<2.0.0
seaborn>=0.13.0,<1.0.0

# Configuration & Utilities
pyyaml>=6.0.0,<7.0.0

# Development Tools
jupyter>=1.1.0,<2.0.0
ipykernel>=6.25.0,<7.0.0
pytest>=8.0.0,<9.0.0
black>=24.0.0,<25.0.0

# Model Context Protocol (MCP) Server
mcp>=1.0.0
pydantic>=2.0.0
anyio>=3.0.0

# (Removed) pip-check-duplicates was referenced but no distribution exists.
# If dependency insight is needed, install pipdeptree instead:
# pipdeptree>=2.16.0
