[build-system]
requires = ["setuptools>=84.0.0", "wheel>=0.48.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aria"
version = "0.0.0"
description = "Aria — Interactive AI Character Platform"
requires-python = ">=3.10"
dependencies = [
  "pydantic>=2.13.4",
  "pydantic-settings>=2.15.0",
  "tenacity>=9.1.4",
]

[project.optional-dependencies]
dev = [
  "pytest>=9.1.1",
  "pytest-cov>=7.1.0",
  "pytest-asyncio>=1.4.0",
  "pytest-xdist>=3.8.0",
  "watchdog>=6.0.0",
  "pre-commit>=4.6.2",
  "ruff>=0.16.5",
  "black>=26.5.1",
  "isort>=9.0.1",
  "mypy>=2.3.1",
  "pydantic>=2.13.4",
  "pydantic-settings>=2.15.0",
  "tenacity>=9.1.4",
]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = ["E501"]  # E501 is redundant — Black auto-formats line length

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["F811", "E402"]   # F811 (redefinition of unused name) is intentional for pytest fixtures; E402 for sys.path bootstrap before imports
"scripts/*" = ["T201", "E402"] # allow print() in scripts; E402 for sys.path bootstrap before imports

[tool.black]
line-length = 120
target-version = ["py310", "py311"]
exclude = '''
/(
    \.git
  | \.venv
  | venv
  | data_out
  | mount
  | __pycache__
)/
'''

[tool.isort]
profile = "black"
line_length = 120
skip_glob = ["venv*", "data_out*", "mount*"]

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true
check_untyped_defs = true
warn_unused_ignores = true
warn_return_any = false
exclude = ["data_out/", "mount/", "venv/"]

[tool.setuptools.packages.find]
where = ["."]
include = ["shared*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["ai-projects", "datasets", "data_out", "mount", ".venv", "venv", "node_modules"]
addopts = "-q --tb=short"
asyncio_mode = "auto"
markers = [
  "unit: Unit tests",
  "slow: Slow tests that may take significant time",
  "azure: Tests requiring Azure services",
  "integration: Integration tests",
  "e2e: End-to-end tests",
  "playwright: Tests using Playwright browser automation",
  "pyppeteer: Tests using Pyppeteer browser automation",
  "selenium: Tests using Selenium browser automation",
]
