# Application Insights telemetry
azure-monitor-opentelemetry>=1.8.8,<2.0.0  # Application Insights telemetry
# NOTE: Remove explicit opentelemetry-api/opentelemetry-sdk pins to avoid pip
# dependency resolution conflicts with azure-monitor-opentelemetry. Let the
# Azure package pull compatible OpenTelemetry versions.
# opentelemetry-api>=1.41.1,<2.0.0
# opentelemetry-sdk>=1.41.1,<2.0.0

azure-cosmos>=4.15.0,<5.0.0  # Cosmos DB client

azure-functions
openai>=2.37.0
colorama>=0.4.6
pytest>=8.4.2
pytest-asyncio>=1.2.0
tiktoken>=0.13.0
pyyaml>=6.0.3
pyodbc>=5.3.0  # Database logging (Azure SQL / SQL Server)
sqlalchemy>=2.0.49  # Optional higher-level DB access (future use)

# Core robustness and resilience
pydantic>=2.13.4
pydantic-settings>=2.0.0
tenacity>=9.1.2  # Retry/backoff for provider calls and orchestrator cycles

# Optional relational drivers (uncomment if using these backends)
# psycopg2-binary>=2.9.9  # PostgreSQL support
# mysqlclient>=2.2.4      # MySQL/MariaDB support

# Dashboard / WebSocket
Flask>=3.1.3
flask-socketio>=5.6.1
python-socketio>=5.16.1
eventlet>=0.40.4  # Optional async server for SocketIO (can fallback to threading)
gradio>=5.24.0

# Optional TTS fallbacks for local development: pyttsx3 (offline, Windows-friendly) and gTTS
# If you want server-side TTS without Azure credentials, enable QAI_ENABLE_LOCAL_TTS and
# install these packages.
pyttsx3>=2.99
gTTS>=2.5.4

# Optional evaluation & visualization (confusion-matrix, plots)
# Install these when you want to produce evaluation artifacts:
# scikit-learn, matplotlib, seaborn
scikit-learn>=1.6.1
matplotlib>=3.9.4
seaborn>=0.13.2

# Vision inference dependencies
Pillow>=11.3.0
torch>=2.8.0
numpy>=1.26.4

# JSON schema validation library used by tests and cooking-ai utilities
jsonschema>=4.25.1

# NOTE: After adding telemetry & cosmos, run task: pip install (functions)
